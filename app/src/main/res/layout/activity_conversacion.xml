<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:colorBackground"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- ============ CABECERA ============ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/botonAtras"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/desc_atras"
            android:src="@drawable/ic_atras" />

        <LinearLayout
            android:id="@+id/zonaTitulo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:orientation="vertical"
            android:paddingVertical="4dp">

            <TextView
                android:id="@+id/tituloConversacion"
                style="@style/Nudo.Titular"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textSize="19sp"
                tools:text="Tutoría del TFG con Alberto" />

            <TextView
                android:id="@+id/metaConversacion"
                style="@style/Nudo.Maquina"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textSize="11sp"
                tools:text="16 jul · 20:14 · toca para renombrar" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/botonBorrar"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/desc_borrar"
            android:src="@drawable/ic_borrar" />
    </LinearLayout>

    <!-- ============ HABLANTES ============ -->
    <LinearLayout
        android:id="@+id/grupoHablantes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/etiquetaHablantes"
            style="@style/Nudo.Maquina"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:letterSpacing="0.06"
            android:text="@string/hablantes_ayuda"
            android:textAllCaps="true"
            android:textSize="10sp" />

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/grupoChipsHablantes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            app:chipSpacingVertical="2dp"
            app:singleSelection="false" />
    </LinearLayout>

    <!-- ============ TRANSCRIPCIÓN ============ -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_weight="1">

        <ScrollView
            android:id="@+id/contenedorContenido"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/fondo_tarjeta"
            android:fillViewport="true">

            <TextView
                android:id="@+id/textoContenido"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lineSpacingExtra="5dp"
                android:padding="16dp"
                android:textColor="@color/nudo_texto"
                android:textIsSelectable="true"
                android:textSize="15sp"
                tools:text="00:07  SPEAKER_00  Yo quitaría el estudio de campo." />
        </ScrollView>

        <LinearLayout
            android:id="@+id/grupoEstadoDetalle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/progresoDetalle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:indeterminateTint="@color/nudo_hilo" />

            <TextView
                android:id="@+id/textoEstadoDetalle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:gravity="center"
                android:paddingHorizontal="24dp"
                android:textColor="@color/nudo_texto_suave"
                android:textSize="14sp"
                tools:text="Anudando…" />
        </LinearLayout>
    </FrameLayout>

    <!-- ============ PRÓXIMAMENTE ============ -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/botonProximamente"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/proximamente_boton"
        android:textColor="@color/nudo_texto_suave"
        app:strokeColor="@color/nudo_linea" />
</LinearLayout>
